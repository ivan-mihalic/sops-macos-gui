# Task 2 report — Project recipient registry

- Status: complete
- Commit: initial `70cbcb1 feat(projects): add shared recipient registry`; review fix `fix(projects): validate recipient registry writes`.
- Tests: `swift test --filter RecipientRegistryTests` — 7 passed; `git diff --check` passed.
- Changes: strict local Bech32 decoding requires HRP `age`, checksum `bech32`, canonical 5→8 conversion, and exactly 32 decoded bytes. `loadSnapshot` binds an upsert/remove write expectation to the exact decoded bytes (before/after fingerprint check); save/upsert/remove can also receive an explicit expectation. The second-writer test replaces the registry after its fingerprint was observed, then proves `upsert` refuses it and preserves the external record.
- TDD RED: initial `swift test --filter RecipientRegistryTests` before the first implementation failed with `cannot find 'RecipientRecord' in scope` and `cannot find 'RecipientRegistry' in scope`. For this review round, `swift test --filter RecipientRegistryTests` after adding the new tests failed at `RecipientRegistryTests.swift:102` with `extra argument 'expecting' in call`; the added checksum test therefore could not execute separately against the prior shape-only implementation. A subsequent focused RED command, `swift test --filter RecipientRegistryTests/refusesSecondWriter`, failed with the same expected missing `expecting` argument. This is the exact available RED evidence; no standalone checksum runtime RED was observed because both new tests compiled in the same file.
- GREEN: `swift test --filter RecipientRegistryTests` passed 7/7 after implementation; `git diff --check` passed.
- Self-review: validated strict local decoding without CLI/environment/key files/plugins; external replacements after a caller's observed fingerprint are refused with `AtomicFileWriter.Error.destinationChangedOnDisk` and leave their bytes intact. The snapshot closes replacement between read and fingerprint capture. `RecipientKind` was absent, so the required public enum is defined beside the registry with the design's `device`, `server`, and `person` cases.
